export interface Cost {
  // Food?: number;
  // Gold?: number;
  // Wood?: number;
  [key: string]: number;
}

// wanted to create a generic type for this but it looks like units can have different properties
